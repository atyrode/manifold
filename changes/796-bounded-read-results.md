---
section: Added
issue: 796
---

External agents can receive a bounded, explicitly selected projection of an ordinary action result without receiving its raw result. The action declares primitive leaves and limits; a trusted launcher separately approves the exact door and declaration digest. Authority, policy acknowledgement and durable trace correlation remain required, and returned data is marked untrusted. Both host and runner enforce bounds; an output refusal preserves the successful effect rather than encouraging a duplicate invocation. The real executable is exercised against two unrelated packed plugins in both in-realm and hardened modes, including default-off disclosure, omitted private fields, oversized UTF-8 output, credential carriers and confirmed teardown. Shared protocol 38 and hardened contract 3 add the vocabulary while retaining older accepted machine, instance and hardened contracts.
