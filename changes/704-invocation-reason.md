---
section: Fixed
issue: 704
---

A machine whose child-job invocation is refused now learns which check refused it, instead of the single constant that replaced every distinct reason and reached the workload only as an opaque 503.
