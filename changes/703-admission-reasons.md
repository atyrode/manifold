- A machine now says which admission check refused a job start, and logs the refusal on its own host, instead of recording the single reason `start_not_admitted` for six unrelated faults in a private journal.
