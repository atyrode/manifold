---
section: Added
issue: 800
---

Governed terminals can consume declared, sealed outputs from earlier jobs on the same machine. The native owner mounts the selected material read-only, preserves reviewed input references across ordinary restart, and checks fresh harness launch bindings and source-read authority before execution. Extracted material is released with its consuming job without deleting the sealed source.
