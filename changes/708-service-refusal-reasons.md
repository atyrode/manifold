---
section: Fixed
issue: 708
---

A machine now records which fact stopped a runtime service it would not start, instead of leaving a cancelled child job with empty output and answering the workload one undifferentiated refusal.
