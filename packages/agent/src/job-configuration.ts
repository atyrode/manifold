/** Shared descriptor boundary for trusted local owner provisioning; no execution entry point. */
export { HeldDirectory } from "./job-files.ts";
export { DirectoryExclusions } from "./job-locations.ts";
