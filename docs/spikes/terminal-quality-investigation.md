# Terminal quality investigation — first-session evidence

Date: 2026-09-05. Investigation: [#281](https://github.com/atyrode/manifold/issues/281).

## Decision summary

Keep the current terminal engine while isolating sizing, shared-PTY authority, replay ordering, and flow control. The completed browser experiment demonstrates a host/FitAddon box-model integration mismatch. The completed transport experiment demonstrates that two distinct connections of the same principal can alternately resize the actual shared PTY. Neither result establishes the cause of the user's duplicated TUI lines, nor establishes an engine performance bottleneck.

This is research, not a native-parity claim. The complete beta combines terminal fitting/publication fixes with a separate shared-tile fix: ratio-only layout changes must not animate the scale of live contents. Experiments 3–4 describe the **intermediate terminal-only beta**, which still stretched during dragging; experiment 5 isolates and corrects that regression. Deployment status belongs to [#284](https://github.com/atyrode/manifold/issues/284), not an inference from source presence. The user's Mac/PC repeated-redraw reports remain unresolved. The companion [terminal quality contract](terminal-quality-contract.md) owns desired outcomes and acceptance obligations; [#281](https://github.com/atyrode/manifold/issues/281) stays open for the broader program-neutral initiative.

Evidence labels distinguish **completed observations**, **source findings**, **hypotheses**, and **recommendations**. A matched continuous-drag browser comparison and pinned engine feasibility replay completed. They are not an engine performance ranking. No native Ghostty comparison, physical-display latency, or uncontended performance baseline is reported here.

## Completed experiment 1: browser bottom-edge geometry

This was a manual browser experiment, not an automated benchmark. Session record:

| Variable                            | Observed value                          |
| ----------------------------------- | --------------------------------------- |
| Source revision / runtime version   | `629f91a` / `v0.7.0`                    |
| Host / runtime                      | Linux `6.18.34`, Bun `1.3.13`           |
| Browser                             | Chromium `148`                          |
| CSS viewport / device pixel ratio   | `1440 × 900` / `1.25`                   |
| Composition / terminal font size    | Solo / `13`                             |
| Host bottom / height                | `900` / `834.406` CSS px                |
| Host padding                        | `5.6` CSS px                            |
| xterm screen height / rows / bottom | `832` CSS px / `52` / `903.1875` CSS px |

**Observation:** the xterm screen extended `3.1875` CSS px below the host bottom. Removing **only the host padding at runtime** retained the same `52` rows and moved the screen bottom to `897.59375`, inside the host. The temporary override was removed afterward, restoring the original styling. This intervention did not change the renderer, transport, font size, or product code.

**Source explanation at baseline `629f91a`:** [`styles.css`](../../packages/plugins/terminals/src/styles.css) gave `.xterm-host` `box-sizing: border-box`, `padding: 0.35rem`, and clipped overflow; its child `.xterm` had `height: 100%`. The inspected FitAddon `0.11` source, lines 72–85, uses the parent’s computed height but subtracts the terminal element’s padding, which was `0` in this experiment ([upstream FitAddon source](https://github.com/xtermjs/xterm.js/blob/master/addons/addon-fit/src/FitAddon.ts); the inspected package version is the evidence, not a claim that mutable upstream line numbers are fixed). The beta margin change addresses this demonstrated mechanism; this baseline observation does not by itself verify the changed product across sizes.

Together, the measured intervention and sizing algorithm demonstrate a **box-model integration mismatch**, not proof that xterm intrinsically cannot fit. The observed fit at font size `32` is coincidental row quantization: coarser rows can leave enough remainder to hide a padding error. It is not evidence that the sizing contract is correct at other sizes. The retained row count after padding removal also shows why checking only `rows`/`cols` misses the visible defect.

**Boundary:** this establishes one geometry mechanism at this viewport/DPR/font condition. It does not establish the full divider-drag timeline, all zoom modes, platform rasterization behavior, or the source of repeated TUI lines. Browser zoom, OS scaling/DPR, terminal font zoom, and pane resizing must be separate conditions.

## Completed experiment 2: real PTY and two same-principal SDK sockets

Command for the three standalone runs: `BENCH_RTT_SAMPLES=30 BENCH_RESIZES=20 bun scripts/bench-terminal-transport.ts`. Raw evidence: [run 0](terminal-quality-data/transport-0.json), [run 1](terminal-quality-data/transport-1.json), [run 2](terminal-quality-data/transport-2.json).

The research script creates its own temporary loopback server, enrolled agent, principal grant, composition, and real PTY fixture. Two canonical SDK clients use distinct WebSocket factory identities to bypass pooling, authenticate with the same grant, and explicitly verify both same-principal and distinct-transport identities. This is not two handles sharing one socket and not a mocked PTY. No production session is used.

### Correctness observations

- Each of the **three** runs confirmed two real, distinct SDK sockets with the same principal.
- Each accepted **20** alternating `96 × 28` / `132 × 40` requests: **60 actual-PTY resizes** total. The fixture handles `SIGWINCH`, queries `stty`, and emits a geometry marker; this goes beyond a broker metadata acknowledgment.
- Each completed **30** alternating input samples; observed output sequences were contiguous.
- In each run both viewers received the **139,180-byte**, **512-marker** batch exactly, with expected SHA-256 equality and marker ordering.
- All three runs reported correctness `ok: true`, with no failures or errors.

These observations prove acceptance of competing same-principal size authorities for the bounded alternating workload. They do **not** reproduce simultaneous cross-device drag, demonstrate duplicate rendered rows, or identify the root cause of the user's duplicated UI. Serialized snapshot correctness and byte continuity do not imply correct browser reflow at a different grid.

### Exploratory timings — shared host, not a baseline

The earlier 20-input/12-resize pilot overlapped `tsc`; it is not the three-run series below. The three standalone runs had no internally concurrent `tsc`, but the host was shared, not exclusive. Strong run-to-run variation precludes a no-contention claim, budget, or engine ranking. The raw script's “no-contention” limitation describes no deliberately competing fixture workload, not proof of host isolation.

| Exact endpoint                                                                  | Samples per run | Range of run p50s | Range of run p95s | Range of run maxima |
| ------------------------------------------------------------------------------- | --------------: | ----------------: | ----------------: | ------------------: |
| SDK input send → returned marker at viewer A’s SDK output callback              |              30 |   9.749–43.012 ms | 25.562–101.699 ms |   25.769–137.272 ms |
| SDK resize command → actual-PTY redraw marker at viewer A’s SDK output callback |              20 |  15.015–55.013 ms | 31.291–110.384 ms |   31.738–111.811 ms |

These are ranges across three per-run summaries, **not pooled percentiles**. Senders alternate A/B; each next command waits for both viewers. Resize includes fixture `SIGWINCH` handling and a `stty` subprocess. Input includes command transit, fixture processing, PTY and transport work. Neither includes a browser parser, DOM update, compositor, display scan-out, or physical input-to-photon measurement. Percentiles use nearest-rank sorting. One short batch per run is not sustained bandwidth or queue-saturation evidence.

## Completed experiment 3: baseline and intermediate terminal-only beta

The unmodified baseline bundle was built from `c85512b`; its terminal implementation retains the historical padding and timeout. The intermediate beta added host margin, frame-coalesced publication, and publication-time fitting, **not yet the shared-tile visual fix**. Both builds and their isolated servers used `MANIFOLD_VERSION=0.7.0`, `MANIFOLD_BUILD=terminal-research-281`, and `MANIFOLD_CHANNEL=development`. This explicit lab identity prevents a mismatched-build banner from obstructing the controls; it does not pretend the two source conditions are identical. Bundle entry points were `index-vmRQyp4Y.js` (baseline) and `index-D8o7sIO9.js` (intermediate beta).

Both runs completed with exit 0, **149 trusted intended clicks**, **29 captures**, no harness errors, and acknowledged browser/profile cleanup. Full records are retained losslessly as [baseline JSON.gz](terminal-quality-data/ui-baseline.json.gz) and [beta JSON.gz](terminal-quality-data/ui-beta.json.gz); decompress with `gzip -dc <file>`. The script's `sourceCommit` identifies its backend/driver checkout, not an independently supplied web bundle.

| Observed endpoint                                                                | Unmodified baseline                              | Beta                                                                                                        |
| -------------------------------------------------------------------------------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| Font 13, primary `1440 × 900`, DPR `1.25`: screen bottom relative to host bottom | **3.1875 px outside**, visibly clipped           | **7.21875 px inside**, final glyphs fully visible                                                           |
| Actual grid at that font/inset                                                   | `52` rows, including the clipped row             | `51` valid rows; the preserved inset leaves less usable space than the earlier padding-removal intervention |
| Outbound resize frames during held divider drag                                  | **0** over the `1,383.988 ms` dispatch interval  | **34** over `1,768.391 ms`, **17 per terminal**                                                             |
| Final publication relative to release dispatch completion                        | Two sends **208.339 / 219.158 ms after** release | Final two sends approximately **43 ms before** release; none after release through the settled capture      |
| Settled left-terminal PTY marker / rendered grid                                 | `61 × 52`                                        | `59 × 51`, matching the final published geometry                                                            |

The visible comparison is retained as [clipped baseline](terminal-quality-data/baseline-font-13.png), [fitted beta](terminal-quality-data/beta-font-13.png), and [beta after divider drag](terminal-quality-data/beta-drag-settled.png). Visual inspection found the beta's bottom marker readable with normally proportioned glyphs and no duplicate/overlapping rows in these screenshots. Some continuous-producer captures intersect partial redraws, including missing bottom markers in two beta font-13 captures; those observations are retained, not silently converted into corruption or complete-frame passes.

These are **policy and geometry observations**, not a smoothness speedup ratio. The drag durations differ, the driver adds DOM/dispatch overhead, and the beta does more real resize/reflow work. No screenshots were taken while the divider was held. Received resize events numbered 68 during the beta's held interval; the raw record preserves the broadcast cost. Five input samples gave DOM-observation p50/p95 of `49.3/81.2 ms` versus `37.7/49.9 ms`; that tiny sample does not establish an input-performance improvement. Whole-run rAF distributions include startup and must not be presented as drag-only frame budgets.

The second browser (`1000 × 680`, DPR `2`) deliberately shares the principal. Those captures exercise the known competing-size-authority problem; they do not certify cross-device sizing or explain the operator's duplicate-redraw report.

### Every integer font size, one controller

A separate real-browser sweep used actual mouse clicks on the font controls, verified the committed value, and observed geometry after 120 ms at **every size 8–32**, once at each viewport/DPR above. **50/50** screens stayed within their hosts: maximum signed bottom excess `−0.21875 px`, maximum right excess `−14.453125 px`. [Raw sweep](terminal-quality-data/font-sweep.json). This establishes the sampled DOM geometry with a single controller, not real Mac/PC rasterization, browser zoom coverage, or physical paint timing.

## Completed experiment 4: induced stale-echo ordering

An older `terminal_event resized` can call `terminal.resize(oldCols, oldRows)` after the host fit but before publication. Reading only the mutable xterm grid then loses the intended final geometry.

The controlled browser experiment registered a later ResizeObserver, verified that the product had already fitted the new box, then performed that exact handler operation. It collected actual outgoing resize frames and queried the real PTY with `stty size`. A one-call ablation suppressed only the new publication-time fit, keeping the earlier ResizeObserver fit intact:

| State                                           | Publication-time fit enabled | Only that fit suppressed |
| ----------------------------------------------- | ---------------------------- | ------------------------ |
| Previously published grid                       | `136 × 51`                   | `136 × 51`               |
| New host's proposed grid / observed initial fit | `131 × 51`                   | `131 × 51`               |
| Injected stale grid                             | `136 × 51`                   | `136 × 51`               |
| Outgoing correction                             | Exactly one `131 × 51`       | None                     |
| Settled rendered grid                           | `131 × 51`                   | Stale `136 × 51`         |

The real PTY printed `RACE_GRID 51 131` (rows, then columns) for the corrected case. [Raw ordering evidence](terminal-quality-data/race-order.json). Instrumentation and the temporary width override were removed afterward. This isolates why the added fit is necessary; it is not a natural network-echo trace or a measurement of the race's frequency. It does not change ownership or filter arbitrary “old” output.

## Completed experiment 5: visual divider-resize regression

The operator emphasized that stretching was a **regression**, not an acceptable browser limitation. Source history separates it from the long-standing PTY delay: `75eb186` introduced `TileMotionBoundary`, whose pre-mutation snapshot ran for every new layout object, including continuously changing ratios. Its FLIP animation scaled the live terminal contents after each divider update. The preceding renderer directly seated stable hosts without those animations.

A real 20-step mouse drag against the intermediate beta retained both rAF geometry samples and a held-drag screenshot. A separate run used the complete fix (`index-NBScF-9-.js`):

| Visible geometry during held drag             | Intermediate beta      | Complete fix   |
| --------------------------------------------- | ---------------------- | -------------- |
| Observed horizontal scale                     | `0.904098–1.18689`     | Exactly `1`    |
| Width of the same nominal 8 px terminal glyph | `7.232758–9.495117 px` | Exactly `8 px` |
| Held host samples                             | `110`                  | `68`           |

[Before trace](terminal-quality-data/stretch-before.json), [after trace](terminal-quality-data/stretch-after.json), [before screenshot](terminal-quality-data/stretch-before-held.png), [after screenshot](terminal-quality-data/stretch-after-held.png). The screenshots contain sparse prompts, so the numerical glyph/transform trace is stronger evidence of distortion than letter-shape inspection. One screenshot was taken mid-drag in each run: this is a visual correctness experiment, not a cadence or native-latency comparison.

The fix reuses the existing `layoutRevision` definition of **structure and occupancy, excluding proportions**. Ratio-only updates do not start settlement animation and cancel interpolation left from a previous settlement; structural placement and carry previews retain their existing animation. There is no local/remote branch and no terminal-specific exception in the shared tile renderer.

A permanent browser-boundary check in `verify-tile-drop.ts` drives ratios from an SDK producer while the receiving browser samples every frame. Against the intermediate bundle it failed with **31/33 moved frames**, despite actual layout width changing from `557` to `735 px`. Against the complete bundle it passed with **0/33 moved frames** over the same width change. The rest of the tile-drop gate also passed, including structural motion, transform-without-PTY-reflow during previews, stable xterm hosts, cancellation, multiplayer equivalence and engaged-portal divider interaction.

The complete bundle also finished the combined UI diagnostic with exit 0, 149 trusted intended clicks, 29 captures and successful cleanup. It sent **38 resize frames during the held drag (19 per terminal)**, with the final sizes sent about **34 ms before release**, and none afterward through the settled capture. Font-13 screens remained `7.21875 px` inside their hosts. [Complete-run JSON.gz](terminal-quality-data/ui-final.json.gz). This run is separate from, and does not overwrite, the intermediate results.

The intermediate PTY-update A/B was therefore necessary but **insufficient** evidence for the user's visible complaint. This record preserves that distinction rather than retroactively claiming the first fix solved stretching.

## Source findings and remaining causal hypotheses

| Source finding                                                                                                                                                                                                                                               | What follows, and what remains unproven                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Baseline `scheduleResize`/`fitAndScheduleResize`: local FitAddon fitting followed by a resettable **250 ms** timeout before shared PTY publication.                                                                                                          | The completed browser A/B confirms no baseline publication during continuous drag and ongoing beta publication. The beta uses one pending animation frame and re-measures at publication; the induced ordering experiment proves why reading a stale echoed grid is insufficient. This does not establish every reported symptom's cause. |
| [`terminal-broker.ts`](../../packages/server/src/terminal-broker.ts), resize/controller path: authority checks the owner **principal**, not a connection-scoped size owner; resize acknowledgment is broadcast after machine send, not after PTY completion. | The completed two-socket run confirms the shared-authority consequence. Acknowledgment alone is not evidence of redraw. Which device should own size is a product decision, not an engine choice.                                                                                                                                         |
| Browser snapshot handler resets an already-painted terminal, replays snapshot bytes, then schedules fit/refresh across animation frames. It filters buffered output by snapshot sequence and ignores already-written sequences.                              | There is real duplicate protection; do not claim every replay blindly appends output. **Hypothesis:** an already-fitted viewer can reset/replay at geometry inconsistent with serialized cursor/wrap assumptions.                                                                                                                         |
| Snapshot messages carry sequence/data but no geometry epoch; agent mirror writes are asynchronous, and resize/snapshot processing has separate scheduling boundaries.                                                                                        | Output sequence is not a geometry version. **Hypothesis:** resize, parser drain, snapshot creation and replay can disagree on grid ordering. No completed corruption trace proves this failure.                                                                                                                                           |
| Broker pending attach queues, session queues, agent socket thresholds, output ring and snapshot limits are bounded. The browser output path writes to xterm without parser-drain-driven upstream flow control.                                               | Existing bounds must not be described as absent. **Hypothesis:** independently bounded queues and asynchronous parser work leave slow-reader/saturation gaps without coordinated end-to-end flow control. The modest completed batch did not exercise those gaps.                                                                         |
| Existing [`terminal.test.ts`](../../packages/testkit/e2e/terminal.test.ts) coverage exercises lifecycle, different-principal control, one resize and snapshot-tail continuity.                                                                               | It does not establish same-principal independent-device sizing or absence of duplicate rendered TUI rows. The added tile-drop browser check covers the separate ratio-animation regression, not these unresolved stream/authority questions.                                                                                              |

The existing idle browser budget gate and [`s2-pty-backpressure.ts`](s2-pty-backpressure.ts) remain useful but answer different questions: idle scheduling/resource behavior and local command-to-marker completion, respectively. Neither is a physical-paint or comprehensive interactive-terminal benchmark.

## Engine decision matrix — source research and bounded feasibility

| Candidate                            | Strengths supported by primary sources                                                                                                                                                                                                                                                                                                         | Risks / recommendation                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Current xterm.js 6, DOM renderer** | Complete browser terminal component with selection, IME, accessibility facilities and addon ecosystem. DOM is the standard/fallback renderer ([source](https://github.com/xtermjs/xterm.js/blob/master/src/browser/renderer/dom/DomRenderer.ts), [docs](https://xtermjs.org/docs/)).                                                           | Its own source says DOM is not designed to be particularly fast. That is not evidence that it bottlenecks this workload. Lowest integration cost; fix sizing/ordering first and profile.                                                                                                                                                                                                                                                                      |
| **xterm.js + WebGL addon**           | Optional WebGL2 renderer and custom glyph rendering; explicit context-loss hooks ([addon and recovery guidance](https://github.com/xtermjs/xterm.js/tree/master/addons/addon-webgl)).                                                                                                                                                          | Requires lifecycle/context-loss disposal and fallback verification. It changes rendering, not PTY ownership or snapshot ordering. Best first renderer A/B candidate without replacing parser/state.                                                                                                                                                                                                                                                           |
| **Coder ghostty-web v0.4.0**         | Downstream Ghostty Wasm parser, xterm-shaped API and Canvas2D renderer; release lists IME, grapheme and selection work ([repository](https://github.com/coder/ghostty-web), [release](https://github.com/coder/ghostty-web/releases/tag/v0.4.0), [renderer](https://github.com/coder/ghostty-web/blob/main/lib/renderer.ts)).                  | Pre-1.0 compatibility is a hypothesis to exercise, not drop-in parity. Open reports include [screen-reader support](https://github.com/coder/ghostty-web/issues/187), [CSP/Wasm loading](https://github.com/coder/ghostty-web/issues/188), [render-loop failure](https://github.com/coder/ghostty-web/issues/189), and [requested WebGL](https://github.com/coder/ghostty-web/issues/155). Suitable isolated pinned spike; not Ghostty’s native GPU renderer. |
| **Direct libghostty-vt Wasm**        | Official parsing/state, reflow, input encoding and render-state APIs; genuine Wasm utilities ([API](https://libghostty.tip.ghostty.org/), [Wasm](https://libghostty.tip.ghostty.org/group__wasm.html), [render state](https://libghostty.tip.ghostty.org/group__render.html), [keyboard](https://libghostty.tip.ghostty.org/group__key.html)). | Unstable API; low-level ABI/memory ownership. Browser renderer, selection UI, accessibility, IME mapping, clipboard, asset/CSP handling and lifecycle remain embedder work. Very high ownership cost; not a justified product replacement without measured need.                                                                                                                                                                                              |

Native [Ghostty](https://github.com/ghostty-org/ghostty#competitive-performance) uses platform graphics and a native application architecture. Its Metal/OpenGL/threading performance claims do not transfer to Wasm/Canvas2D in Chromium. None of these engines decides which browser may resize a shared PTY.

### Completed pinned engine feasibility replay

[Raw engine evidence](terminal-quality-data/engine-run.json) records published **xterm 6.0.0**, **WebGL addon 0.19.0**, and **ghostty-web 0.4.0**, dependency integrity, implementation sources and Chromium `148.0.7778.215`. This isolated synthetic replay used no Manifold transport, PTY, network stream, native Ghostty, or production terminal.

- xterm DOM and ghostty-web Canvas2D each completed **six trials**, three at `80 × 24` and three at `132 × 40`. Each trial replayed `10,446` or `10,447` UTF-8 bytes: 240 colored scrolling lines followed by cursor redraw, wide and combining text. Both passed public-buffer end-marker and cursor-redraw assertions in every trial.
- xterm preserved `e` plus combining acute in all six public-buffer checks. ghostty-web's public-buffer translation returned `e` without the acute in all six, failing that assertion. **This establishes a public-buffer text discrepancy, not visible glyph loss**: visual inspection of the retained [xterm](terminal-quality-data/xterm-dom-80x24.png) and [ghostty-web](terminal-quality-data/ghostty-canvas2d-80x24.png) screenshots found a visible acute accent in both. Copy/selection behavior was not exercised, so those consequences remain to be tested. Do not silently classify the run as full correctness parity.
- xterm's callback follows parser processing; ghostty-web 0.4.0 writes synchronously to Wasm and schedules its callback with `requestAnimationFrame`. Recorded callback spans therefore have different endpoints and are **not comparable parser-latency rankings**. There was no warmup/statistical-significance or physical-paint claim.
- WebGL2 initialization refused under `--disable-gpu` with `WebGL2 not supported null`. There are **no WebGL performance numbers and no context-loss/fallback test result**. This software/headless configuration cannot decide GPU renderer quality.
- Synthetic ArrowUp encoding and input-hook introspection are not native keyboard, IME, or assistive-technology verification. The bounded replay leaves real-TUI compatibility, reflow, selection, accessibility, CSP and recovery obligations open.

Retain one integration unless evidence justifies a replacement. Prefer an upstream wrapper fix over a fork. A fork would require an immutable base, small explicit delta, upstreaming path, owner and update policy; owning a new browser terminal layer or a Ghostty ABI patch is a separate commitment, not a quick performance fix.

## Prioritized recommendations

1. **P0 — correct fit and live resize.** The bounded beta now has matched browser A/B, inspected screenshots, all-integer 8–32 font sweeps at two DPR/viewport conditions, and induced stale-echo ordering evidence. Deliver it through #284 and verify development. Real Mac/PC interaction, browser zoom and broader redraw correctness remain open; the local results are not native-parity certification.
2. **P0 — decide input authority separately from a size lease.** Resolve the policy in [#204](https://github.com/atyrode/manifold/issues/204): shared input permission must not implicitly make every same-principal connection an equally authoritative size writer. Specify lease identity/lifecycle, transfer, disconnect and spectator behavior, and what non-owning viewports display. The transport run demonstrates why this decision is necessary; it does not select the policy.
3. **P0 — geometry epoch/order and replay correctness.** Trace PTY bytes, output sequence, accepted geometry, mirror drain, snapshot watermark and browser writes through resize/reattach. Make any eventual protocol change preserve an explicitly ordered grid and output history. Diagnose repeated lines by finding the first boundary where duplication appears, not by hiding rows or discarding unproven “duplicate” data.
4. **P0 — bounded end-to-end flow.** Inventory existing queue bounds and failure recovery, then exercise slow readers, parser backlog, reconnect and sustained output. Design parser-completion-aware flow only from measured queue behavior; xterm’s [flow-control guide](https://xtermjs.org/docs/guides/flowcontrol/) explains why `write` completion and PTY pause/resume/ACK watermarks matter. Do not claim the current exact-byte batch proves saturation safety.
5. **P1 — renderer A/B after the integration diagnosis.** Extend the completed bounded DOM/ghostty-web feasibility replay with actual copy/selection checks for the public-buffer combining-text discrepancy and real-TUI traces. The retained screenshots render the accent in both engines. Retry WebGL on a suitable GPU configuration, including forced context loss/fallback; the refused initialization is not that test. Compare correctness first and parser/scheduling endpoints separately. No product migration or dependency addition is recommended on the present evidence.
6. **P1 — broader shell and file workflow quality.** Use the quality contract for selection/copy/paste, links, shell cwd and prompt/command integration, keyboard/IME, Unicode, search/scrollback, accessibility, file upload/download and drag/drop expectations. Clipboard is one dimension, not the whole investigation. Distinguish shell-provided metadata from heuristics, user-authorized file transfer from terminal byte input, and local browser files from remote paths. No implementation completeness or browser/native equivalence has been measured here.

## Ranked terminal workflow backlog

These are **proposals**, not achieved behavior. The product is a terminal: shells, editors, TUIs, Codex, Claude Code and other programs are workloads, not alternative core architectures. Any program-specific compatibility belongs in an optional adapter. The [quality contract](terminal-quality-contract.md) separates hard outcomes from future capabilities and worthwhile extensions; the ordering here follows correctness work above.

| Rank                                | Proposed outcome / boundary                                                                                                                                                                                                                                                                                                  | Primary native workflow reference                                                                                                                                                                                                        |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — everyday terminal usability     | Reliable selection/copy/paste, keyboard/IME, Unicode, accessibility and bounded scrollback/search; add viewer-local Find, copy mode and quick-select without persisting terminal content. Native behavior is a design reference, not browser parity already demonstrated.                                                    | [WezTerm copy mode](https://wezterm.org/copymode.html), [quick-select](https://wezterm.org/quickselect.html), [Ghostty keybinding actions](https://ghostty.org/docs/config/keybind/reference)                                            |
| 2 — shell context, opt-in           | Parse bounded, allowlisted OSC 7/133 metadata for cwd, prompt/output zones, command-output copy and prompt navigation. Treat it as untrusted, ephemeral context, never authorization or reliable command truth for arbitrary programs. Notifications can follow semantic completion markers rather than output-text guesses. | [Ghostty shell integration](https://ghostty.org/docs/features/shell-integration), [WezTerm shell integration](https://wezterm.org/shell-integration.html), [kitty shell integration](https://sw.kovidgoyal.net/kitty/shell-integration/) |
| 3 — links and remote file workflows | Explicit link opening with target-machine/path authorization and line/column support; distinguish local browser files, remote paths, clipboard data and uploads/downloads. A user-authorized authenticated transfer service is separate from typing bytes into a shell. Never auto-open or trust terminal-supplied paths.    | [kitty open actions](https://sw.kovidgoyal.net/kitty/open_actions/), [iTerm2 OSC 8/file links](https://iterm2.com/documentation-escape-codes.html), [iTerm2 transfer utilities](https://iterm2.com/documentation-utilities.html)         |
| 4 — worthwhile extensions           | Opt-in completion notifications, bounded local highlight/trigger rules and collaborator follow views that do not acquire input or size authority. Images and proprietary file escapes require explicit compatibility/security decisions, not accidental handling through text paste.                                         | [iTerm2 shell integration](https://iterm2.com/documentation-shell-integration.html), [triggers](https://iterm2.com/documentation-triggers.html), [kitty clipboard protocol](https://sw.kovidgoyal.net/kitty/clipboard/)                  |

Native integrations can supply useful command/cwd semantics and file-transfer affordances, but browser permissions and remote authorization remain separate obligations. No personal clipboard payloads or terminal captures are needed for these research records.

## Durable experiment ledger

The baseline web bundle/manual observation is **`629f91a` / `v0.7.0`**. Transport JSON also records `sourceCommit: 629f91a7ea29f8648178d126fda9e7a51e4a442c`; preserve that recorded identity. Later driver/backend work ran from the **`c85512b` main working tree**, including a minor identity-reporting change. That later harness/backend context is not the source revision of the baseline web bundle and does not retroactively relabel existing JSON. Published engine dependency pins are independent of both application revisions.

| Experiment / command                                                                                          | Source and status                                                                                                                                                            | Evidence / limitations                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Manual browser padding intervention; runtime removal of host padding, then restoration                        | Baseline `629f91a`; completed geometry observation                                                                                                                           | [Padding experiment](terminal-quality-data/padding-experiment.json); one viewport/DPR/font, not continuous drag or changed-product verification                                                                                                                                                                          |
| `BENCH_RTT_SAMPLES=30 BENCH_RESIZES=20 bun scripts/bench-terminal-transport.ts`, three standalone invocations | Recorded `629f91a`; all three bounded correctness runs passed                                                                                                                | [transport-0](terminal-quality-data/transport-0.json), [transport-1](terminal-quality-data/transport-1.json), [transport-2](terminal-quality-data/transport-2.json); shared host, SDK callback endpoints, no browser/native/saturation claim                                                                             |
| `bun scripts/bench-terminal-engines.ts --keep-artifacts`                                                      | xterm `6.0.0`, WebGL addon `0.19.0`, ghostty-web `0.4.0`, isolated verified download pins; DOM/Canvas2D replay completed with combining-text discrepancy; WebGL2 unavailable | [Engine run](terminal-quality-data/engine-run.json); callback endpoints differ; both inspected screenshots show the acute accent; no GPU/context-loss result                                                                                                                                                             |
| `BENCH_TERMINAL_KEEP=1 bun scripts/bench-terminal-ui.ts` diagnostic entry point                               | Baseline JSON records `629f91a`; near-completed workload, **invalid harness result** after profile cleanup errors                                                            | Initial `ui-baseline.json` was inspected but is not promoted as valid baseline evidence; screenshots between drag steps perturb the workload                                                                                                                                                                             |
| Later UI baseline rerun, same diagnostic family                                                               | Failed font-control settlement; profiles cleaned correctly on this attempt                                                                                                   | Trusted pointer diagnostics identified an old-bundle/new-server build-mismatch banner intercepting the clicks. This is an invalid mixed-build harness configuration, not a terminal-button defect. Matched baseline and beta builds use the same explicit lab identity while recording their distinct source conditions. |
| Matched `c85512b` baseline and intermediate terminal-only beta, explicit identical lab identity               | Both complete, exit 0; 149 trusted intended clicks and 29 captures each                                                                                                      | [Baseline JSON.gz](terminal-quality-data/ui-baseline.json.gz), [intermediate JSON.gz](terminal-quality-data/ui-beta.json.gz); PTY publication and clipping evidence, **not visual-stretch acceptance**                                                                                                                   |
| Single-controller all-font sweep and induced stale-echo ordering                                              | 50 geometry samples passed; ablation reproduced lost final geometry, corrected case reached real PTY                                                                         | [Font sweep](terminal-quality-data/font-sweep.json), [ordering record](terminal-quality-data/race-order.json); specific functional evidence, no full-contract certification                                                                                                                                              |
| Real held-divider glyph trace and tile-drop regression oracle                                                 | Intermediate bundle fails visual contract; complete bundle holds glyph width and passes tile-drop gate                                                                       | [Before](terminal-quality-data/stretch-before.json), [after](terminal-quality-data/stretch-after.json); no claimed native/cadence speedup                                                                                                                                                                                |

The linked JSON records are synthetic experiment data, not production sessions. Preserve failures and environment limitations alongside successful markers. Later beta measurements must record their own source/build identity and must not overwrite this baseline series.

## Reproduction commands and evidence discipline

Run research scripts **serially**, with no gates, compiler, builds, or other benchmark competing during the measurement window. These commands are runnable entry points, not claims that every entry point has completed successfully:

```sh
bun scripts/bench-terminal-transport.ts
bun scripts/bench-terminal-ui.ts --help
bun run changelog:generate
BENCH_TERMINAL_REPORT=/tmp/terminal-ui.json BENCH_TERMINAL_KEEP=1 bun scripts/bench-terminal-ui.ts
```

Both experiments target their own localhost infrastructure, not a production endpoint. The transport script requires installed repository Bun dependencies and POSIX `stty`. It supports bounded `BENCH_RTT_SAMPLES`, `BENCH_RESIZES`, `BENCH_BULK_LINES`, `BENCH_BULK_WIDTH` and `BENCH_TIMEOUT_MS` overrides; defaults are `20`, `12`, `512`, `256` and `10000` respectively.

The UI diagnostic requires Chromium and repository dependencies; generate the in-app changelog in a fresh checkout before building. `MANIFOLD_CHROMIUM` selects the browser. `MANIFOLD_GATE_DIST` can reuse an existing build; without it, the diagnostic prepares an isolated bundle before measurement. A reused bundle and server must agree on `MANIFOLD_VERSION`, `MANIFOLD_BUILD` and `MANIFOLD_CHANNEL`. `BENCH_TERMINAL_REPORT` selects the atomically written raw JSON file; stdout is a bounded summary naming it, not the full record. `BENCH_TERMINAL_KEEP=1` retains screenshots in the recorded artifact directory while server data and browser profiles are still cleaned. Earlier cleanup, mixed-build and truncated-output failures remain invalid attempts; the matched, complete runs above supersede them without treating their timing as valid evidence.

Retain each run’s JSON separately with source commit, runtime version, timestamp, workload parameters, units, raw samples, counts, correctness failures and environment. Record browser version/flags, viewport/DPR/zoom, font, renderer, CPU/GPU/display/refresh/power state, topology and contention where relevant; an absent value is unknown, not an inferred default. Never retain access tokens, owner keys or production terminal content. The short source/runtime pair above identifies the manual browser observation, not an invented complete metadata record for other runs.

Endpoints must stay distinct:

- SDK output callback: transport arrival, no browser rendering.
- xterm write callback: parser completion, not display presentation.
- DOM text or geometry observation: browser state, not physical paint.
- `requestAnimationFrame`: next render opportunity, not proof of scan-out ([MDN](https://developer.mozilla.org/en-US/docs/Web/API/Window/requestAnimationFrame)).
- Event Timing: rounded browser interaction timing with event/threshold constraints ([specification](https://www.w3.org/TR/event-timing/)); not a hardware input timestamp.
- Physical input-to-photon: requires calibrated camera/photodiode and actual input/display references. No such measurement was performed.

## Network and browser limits versus fixable integration

**Theoretical network constraint:** when a visible response depends on remote PTY processing, input must traverse the outbound path and output the return path, plus server/agent processing and browser presentation. RTT, jitter, queueing and application work therefore bound the achievable remote response; changing a parser cannot remove that transit. No numeric WAN lower bound or one-way delay was measured here. Do not subtract timestamps from unsynchronized Mac/PC clocks; measure same-clock round trips or clearly defined individual legs.

**Documented browser constraints:** [clipboard](https://developer.mozilla.org/en-US/docs/Web/API/Clipboard_API) access depends on secure contexts, permissions/user activation and browser differences. OS/browser-reserved shortcuts and [IME composition](https://developer.mozilla.org/en-US/docs/Web/API/Element/compositionstart_event) are not equivalent to raw native terminal key events. Browser file access requires user authorization; a local browser path is not a remote-agent filesystem path. Background scheduling throttles timers and pauses most rAF work ([Chrome policy](https://developer.chrome.com/blog/background_tabs)). These are platform constraints to design around, not measurements of current Manifold failures. Permissions, clear affordances and application wiring still remain fixable product responsibilities.

**Actual Mac/PC display tests are required.** This Linux/Chromium evidence cannot clear native macOS or PC GPU/compositor behavior, physical refresh/VRR, font rasterization, keyboard layouts, dead keys/IME, clipboard prompts, file dialogs or physical latency. Repeat sizing, real-TUI redraw, input, selection and file/shell workflows on the intended foreground browsers and physical displays, then repeat the shared terminal with the intended network topology. Compare native Ghostty on each same host only as a separately labeled native/local-PTY baseline. No native-parity conclusion follows from localhost bytes, headless timing, or the choice of parser library.
