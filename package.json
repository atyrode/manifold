{
  "name": "manifold",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/plugins/*"
  ],
  "engines": {
    "bun": ">=1.3.13"
  },
  "scripts": {
    "check": "tsc -p packages/protocol && tsc -p packages/plugin && tsc -p packages/scene && tsc -p packages/sdk && tsc -p packages/server && tsc -p packages/agent && tsc -p packages/testkit && tsc -p packages/web && tsc -p packages/plugins/shell && tsc -p packages/plugins/plugin-manager && tsc -p packages/plugins/terminals && tsc -p packages/plugins/presence && tsc -p packages/plugins/machines && tsc -p packages/plugins/index && tsc -p packages/plugins/draw && tsc -p packages/plugins/notes && tsc -p packages/plugins/uri && tsc -p packages/plugins/access && tsc -p packages/plugins/events && tsc -p packages/plugins/canvas && tsc -p packages/plugins/compositions && tsc -p tsconfig.scripts.json",
    "test": "bun test packages/protocol packages/plugin packages/scene packages/sdk packages/server packages/agent packages/web packages/plugins",
    "e2e": "bun test packages/testkit --timeout 60000",
    "verify:convergence": "bun scripts/verify-convergence.ts",
    "verify:public": "bun scripts/verify-public.ts",
    "verify:terminal-selection": "bun scripts/verify-terminal-selection.ts",
    "verify:terminal-mirror": "bun scripts/verify-terminal-mirror.ts",
    "verify:tile-drop": "bun scripts/verify-tile-drop.ts",
    "verify:axioms": "bun scripts/verify-axioms.ts",
    "changelog:generate": "bun scripts/generate-web-changelog.ts",
    "changelog:check": "bun scripts/generate-web-changelog.ts --check",
    "release": "bun scripts/release.ts",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:server": "MANIFOLD_ANNOUNCE_KEY=1 bun --watch packages/server/src/main.ts",
    "dev:web": "bun run --cwd packages/web dev",
    "build:web": "bun run --cwd packages/web build",
    "gate": "bun scripts/gate.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@types/bun": "^1.3.2",
    "eslint": "^9.39.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.68.0"
  },
  "patchedDependencies": {
    "@xterm/xterm@6.0.0": "patches/@xterm%2Fxterm@6.0.0.patch"
  }
}
